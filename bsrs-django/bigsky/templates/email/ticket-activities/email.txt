{% load contact_tags %}

{% for a in ticket.activities.all %}

    When: {{a.created}};
    Who: {{a.person.fullname}};
    Type: {{a.type.name}};
    What: {{a|ticket_activity_description}}
    {{''|linebreaks}}

{% endfor %}