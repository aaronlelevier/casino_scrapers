{{#if title}}

  <label class="control-label" for={{for}}>{{unless noTranslate (t title) title}}{{#if optional}}<span class="optional">{{t "general.optional"}}</span>{{/if}}
  </label>


{{/if}}

{{yield}}
