{{#power-select
  class=className
  selected=selected
  onchange=(action "select")
  searchEnabled=false
  options=options
  as |option|
}}
  {{if isObject (get option field) (t option)}}
{{/power-select}}
