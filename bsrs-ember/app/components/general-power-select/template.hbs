{{#power-select
  class=className
  tagName="div"
  selected=selected
  onchange=(action "select")
  searchEnabled=false
  options=options
  as |option|
}}
  {{if isObject (get option field) (t option)}}
{{/power-select}}
