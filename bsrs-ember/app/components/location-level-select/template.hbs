{{#power-select
  class="t-location-level-select"
  options=options
  selected=selected
  onchange=(action "selected")
  searchEnabled=false
  allowClear=true
  placeholder="Select One"
  as |location_level|
}}
  {{location_level.name}}
{{/power-select}}
