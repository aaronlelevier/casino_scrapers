{{#power-select
  class="t-location-level-select"
  options=options
  selected=selected
  onchange=(action "selected")
  searchEnabled=false
  placeholder="Select One"
  searchField="name"
  as |location_level|
}}
  {{location_level.name}}
{{/power-select}}
