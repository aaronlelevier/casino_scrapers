{{#power-select
  selected=ticket_assignee_selected
  options=options
  class="t-ticket-assignee-select"
  search=(action "update_filter")
  onchange=(action "selected")
  placeholder="Please select an assignee"
  as |assignee|
}}
  {{assignee.fullname}}
{{/power-select}}
