{{#power-select
  selected=selectedAttr
  class=className
  tagName="div"
  search=(perform searchRepo)
  selectedItemComponent=selectedComponent
  onchange=(action "selected")
  placeholder=placeholder
  as |optionModel|
}}
  {{component componentName model=optionModel}}
{{/power-select}}
