{{#power-select
  class="t-locale-select"
  options=locales
  selected=person.locale
  onchange=(action "selected")
  searchEnabled=false
  as |locale|
}}
  {{locale.name}} - {{locale.native_name}}
{{/power-select}}
