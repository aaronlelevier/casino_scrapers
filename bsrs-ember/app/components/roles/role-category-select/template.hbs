{{#power-select-mobile
  multiple=true
  selected=categories_selected
  options=options
  onchange=(action "change_category")
  class="t-role-category-select"
  tagName="div"
  onopen=(action "handleOpen")
  searchEnabled=false
  labelText=(t "admin.role.label.category")
  as |category|
}}
  {{category.name}}
{{/power-select-mobile}}
