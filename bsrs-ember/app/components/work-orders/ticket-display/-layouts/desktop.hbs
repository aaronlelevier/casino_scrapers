{{#unless isExpanded}}
  {{work-orders/ticket-display-collapsed
    model=model
    indx=indx
    isExpanded=isExpanded
  }}
{{else}}
  {{work-orders/ticket-display-expanded
    model=model
    indx=indx
    permissions=permissions
    isExpanded=isExpanded
  }}
{{/unless}}
