{{#power-select
  class="t-ticket-status-power-select"
  options=statuses
  selected=ticket.status
  onchange=(action "selected")
  searchEnabled=false
  as |status|
}}
  {{status.name}}
{{/power-select}}
