{{#power-select
  multiple=true
  selected=ticket.cc
  search=(action "update_filter")
  onchange=(action "change_cc")
  class="t-ticket-cc-select"
  searchField="name"
  as |person|
}}
  {{person.fullname}}
{{/power-select}}
