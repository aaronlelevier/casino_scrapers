{{#power-select
  multiple=true
  selected=ticket.cc
  options=ticket_cc_options
  class="t-ticket-cc-select"
  search=(action "update_filter")
  onchange=(action "add")
  searchPlaceholder="Type a person"
  as |person|
}}
  {{person.fullname}}
{{/power-select}}

