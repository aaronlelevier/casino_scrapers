{{#form-group title=(concat "admin.person.label." field) optional=optional for=field}}
  {{validated-input
    model=model
    placeholder=(t (concat "admin.person.label." field))
    valuePath=field
    className=(concat "t-person-" dasherizedField)
    class=(concat "t-" dasherizedField "-validator")
    maxlength=maxlength
  }}
{{/form-group}}
