{{#each saved as |filterset|}}
<li><a href="#" onclick={{route-action "apply_filterset" filterset.endpoint_name (filterset-params filterset)}}>{{filterset.name}}<i class="fa fa-minus-circle t-remove-filterset" aria-hidden="true" {{action "removeFilterSet" filterset.id}}></i></a></li>
{{/each}}
