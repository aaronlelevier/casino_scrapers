{{#power-select
  class=(concat relatedModelName '-select' ' t-' relatedModelName '-select')
  selected=selected
  onchange=(action "selected")
  searchEnabled=false
  options=relatedModels
  as |optionModel|
}}
  {{unless notTranslated (t (get optionModel displayName)) (get optionModel displayName)}}
{{/power-select}}
