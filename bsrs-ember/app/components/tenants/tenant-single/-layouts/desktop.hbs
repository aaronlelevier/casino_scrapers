{{base-component
  model=model
  save="save"
  cancel="cancel"
  delete=delete
  btnDisabled=saveTask.isRunning
}}

<section class="docSection container-fluid">
  <div class="row">
    <div class="col-md-12">
      <section style="padding-bottom: 0">
        <div class="row card-title">
          <h1 class="col-md-12 t-tenant-header">
            <span class="new">
            {{#if model.new}}
              {{t "tenant.new"}}
            {{else}}
              {{t "tenant.detail"}}
            {{/if}}
            </span>
          </h1>
        </div>
      </section>
    </div>
    <div class="col-md-12">
      <section>
        <div class="row">
          <div class="col-md-12">
            {{#form-group title="tenant.company_name" for="company_name"}}
              {{validated-input
                model=model
                valuePath="company_name"
                didValidate=didValidate
                className="form-control t-tenant-company_name"
                placeholder=(t "tenant.company_name")
              }}
            {{/form-group}}
          </div>
        </div>
        <div class="row">
          <div class="col-md-12 t-currency-code">
            {{#form-group title="admin.tenant.currency" for="currency"}}
              {{power-select-foreign-key
                mainModel=model
                selected=model.currency
                change_method='change_currency'
                relatedModels=currencies
                relatedModelName='currency'
              }}
            {{/form-group}}
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            {{#form-group title="admin.tenant.countries" for="country"}}
            {{db-fetch-multi-select
              model=model
              multiAttr='country'
              multiAttrIds='country_ids'
              selectedAttr=model.country
              className="t-tenant-country-select"
              displayName="name"
              add_func="add_country"
              remove_func="remove_country"
              repository=countryRepo
              searchMethod="findCountry"
            }}
            {{/form-group}}
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            {{#form-group title="admin.tenant.implementation_email" for="implementation_email"}}
            {{input-single-email 
              model=model 
              email=model.implementation_email
              noDelete=true
            }}
            {{/form-group}}
          </div>
        </div>
        <div class="row">
          <div class="col-md-6">
            {{#form-group title="admin.tenant.billing_email" for="billing_email"}}
            {{input-single-email 
              model=model 
              email=model.billing_email
              noDelete=true
            }}
            {{/form-group}}
          </div>
          <div class="col-md-6">
            {{#form-group title="admin.tenant.billing_phonenumber" for="billing_phonenumber"}}
            {{input-single-phone 
              model=model 
              phonenumber=model.billing_phone
              noDelete=true
            }}
            {{/form-group}}
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            {{#form-group title="admin.tenant.billing_address" for="billing_address"}}
            {{input-single-address 
              model=model 
              address=model.billing_address
              noDelete=true
            }}
            {{/form-group}}
          </div>
        </div>
      </section>
    </div>
  </div>
</section>
