{{#grid-view2 as |t|}}
  <header class="mobile-grid-head t-grid-mobile-header">
    {{t.head
      columns=columns
      page=page
      sort=sort
      find=find
      model=model
      hasActiveFilterSet=hasActiveFilterSet
      grid_title=grid_title
      filtersets=filtersets
      routeName=routeName
      search_placeholder=search_placeholder
    }}
  </header>
  <div class="mobile-grid-body">
    {{t.body
      columns=columns
      model=model
      requested=requested
      detail_url=detail_url
      noun=noun
    }}
  </div>
{{/grid-view2}}
{{infinity-component page=page reachedInfinity=reachedInfinity model=model}}
