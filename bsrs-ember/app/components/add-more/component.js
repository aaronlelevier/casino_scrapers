import Ember from 'ember';

export default Ember.Component.extend({
  tagName: 'button',
  classNames: ['add-more'],
  // click: function(e) {
  //   e.preventDefault();
  //   multi.create().appendTo($('.form-group:last'));
  // }
});
