{{#each paginated_content as |item|}}
  {{#link-to detail_url item.id tagName="div" class="mobile-grid-row t-grid-data"}}
  <hbox>
    {{#each columns as |column|}}
      {{grid/helpers/grid-body-column tagName="div" noun=noun column=column item=item}}
    {{/each}}
  </hbox>
  {{/link-to}}
{{/each}}
