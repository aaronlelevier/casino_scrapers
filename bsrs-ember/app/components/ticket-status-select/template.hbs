{{#power-select
  class="ticket-priority-select t-ticket-status-select"
  options=statuses
  selected=ticket.status
  onchange=(action "selected")
  searchEnabled=false
  as |status|
}}
  {{t status.name}}
{{/power-select}}
