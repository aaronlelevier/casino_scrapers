{{#power-select
  class="t-person-role-select"
  options=options
  selected=model.role
  onchange=(action "selected")
  searchEnabled=false
  placeholder="Select One"
  as |role|
}}
  {{role.name}}
{{/power-select}}
