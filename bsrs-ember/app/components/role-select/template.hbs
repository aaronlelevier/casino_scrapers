{{#power-select
  class="t-person-role-select"
  options=options
  selected=selected
  onchange=(action "selected")
  searchEnabled=false
  placeholder="Select One"
  as |role|
}}
  {{t role.name}}
{{/power-select}}
