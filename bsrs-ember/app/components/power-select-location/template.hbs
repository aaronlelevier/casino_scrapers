{{#power-select
  options=options
  selected=selected
  disabled=disabled
  onchange=(action "change_location")
  repository=repository
  search=(perform searchRepo)
  searchField="name"
  as |location|
}}
  {{location.name}}
{{/power-select}}