{{#power-select
  class="t-location-select"
  tagName="div"
  options=options
  selected=selected
  disabled=disabled
  onchange=(action "change_location")
  search=(perform searchRepo)
  searchField="name"
  as |location|
}}
  {{location.name}}
{{/power-select}}
