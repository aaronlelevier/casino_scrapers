{{#power-select
  class="ticket-priority-select t-ticket-priority-select"
  options=priorities
  selected=ticket.priority
  allowClear=clear
  onchange=(action "selected")
  searchEnabled=false
  as |priority|
}}
  {{t priority.name}}
{{/power-select}}
