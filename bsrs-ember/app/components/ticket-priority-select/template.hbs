{{#power-select
  class="t-ticket-priority-select"
  options=priorities
  selected=ticket.priority
  onchange=(action "selected")
  searchEnabled=false
  as |priority|
}}
  {{t priority.name}}
{{/power-select}}
