{{#power-select
  class="t-ticket-priority-power-select"
  options=priorities
  selected=ticket.priority
  onchange=(action "selected")
  searchEnabled=false
  as |priority|
}}
  {{priority.name}}
{{/power-select}}
