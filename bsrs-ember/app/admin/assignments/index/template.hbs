{{assignments/assignment-list
  count=count
  model=model
  page=page
  sort=sort
  search=search
  find=find
  id_in=id_in
  requested=requested
  page_size=page_size
  filterModel=filterModel
  gridFilterParams=gridFilterParams
  gridIdInParams=gridIdInParams
  filtersets=filtersets
  routeName=routeName
  defaultSort=defaultSort
  noun="assignment"
  plural=(t "admin.assignment.other")
  grid_title=(t "admin.assignment.other")
  list_url="admin.assignments.index"
  detail_url="admin.assignments.assignment"
  add_model_url="admin.assignments.new"
  search_placeholder=(t "admin.assignment.search")
  add_model_text=(t "admin.assignment.one")
  save_filterset=(action "save_filterset")
  hasActiveFilterSet=hasActiveFilterSet
  repository=repository
  exportGrid=(route-action "exportGrid" find search sort)
}}
