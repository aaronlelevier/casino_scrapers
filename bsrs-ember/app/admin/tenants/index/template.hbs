<article class="main">
{{tenants/tenant-list
  model=model
  page=page
  sort=sort
  search=search
  find=find
  id_in=id_in
  requested=requested
  page_size=page_size
  filterModel=filterModel
  gridFilterParams=gridFilterParams
  gridIdInParams=gridIdInParams
  isLoading=isLoading
  filtersets=filtersets
  routeName=routeName
  defaultSort=defaultSort
  noun="tenant"
  plural=(t "tenant.other")
  grid_title=(t "tenant" count="2")
  list_url="admin.tenants.index"
  detail_url="admin.tenants.tenant"
  add_model_url="admin.tenants.new"
  search_placeholder=(t "tenant.search")
  add_model_text=(t "tenant.one")
  save_filterset=(action "save_filterset")
  hasActiveFilterSet=hasActiveFilterSet
  updateGridFilterParams=(route-action "updateGridFilterParams")
  linkToNew=(route-action "linkToNew")
}}
</article>
