<article class="main">
{{location-levels
  model=model
  page=page
  sort=sort
  search=search
  find=find
  isLoading=isLoading
  requested=requested
  page_size=page_size
  filterModel=filterModel
  filtersets=filtersets
  routeName=routeName
  noun="location-level"
  plural=(t "admin.locationlevel.other")
  grid_title=(t "admin.locationlevel" count="2")
  list_url="admin.location-levels.index"
  detail_url="admin.location-levels.location-level"
  add_model_url="admin.location-levels.new"
  search_placeholder=(t "admin.locationlevel.search")
  add_model_text=(t "admin.locationlevel.one")
  save_filterset=(action "save_filterset")
  hasActiveFilterSet=hasActiveFilterSet
  repository=repository
  exportGrid=(route-action "exportGrid" find search sort)
  linkToNew=(route-action "linkToNew")
}}
</article>
