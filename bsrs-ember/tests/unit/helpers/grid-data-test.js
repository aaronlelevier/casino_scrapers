import { gridData } from '../../../helpers/grid-data';
import { module, test } from 'qunit';

module('Unit | Helper | grid data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gridData(42);
  assert.ok(result);
});
