import { tabWidth } from 'bsrs-ember/helpers/tab-width';
import { module, test } from 'qunit';

module('Unit | Helper | tab width');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tabWidth([42]);
  assert.ok(result);
});
