import STATES from '../../vendor/state_fixtures';

var StatesFactory = {
    list: function() {
        return STATES; //TODO: updated the mocks in server/mocks
    }
};

export default StatesFactory;
